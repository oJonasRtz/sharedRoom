<VirtualHost *:80>
	ServerName localhost

	ProxyPreserveHost On

	ProxyPass "/" "http://web:5117/"
	ProxyPassReverse "/" "http://web:5117/"


	#https redirect
	# Redirect permanent / https://localhost/
</VirtualHost>

# Config Https later
# <VirtualHost *:443>
	# SSLEngine on
	# SSLCertificateFile /path/to/cert.pem
	# SSLCertificateKeyFile /path/to/key.pem

	# SSLProxyEngine On
	# ProxyPreserveHost On

	# ProxyPass "/" "http://web:5117/"
	# ProxyPassReverse "/" "http://web:5117/"

# </VirtualHost>
